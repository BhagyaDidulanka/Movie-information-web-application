import { Card } from '../components/Card';

export const MovieList = () => {
  return (
    <main>
          <section class="flex flex-wrap gap-6 py-9 justify-center">
            <Card/>
          </section>
    </main>
  )
}
